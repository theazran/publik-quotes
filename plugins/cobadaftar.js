// const { createHash } = require('crypto')
// let handler = async function (m, { text, usedPrefix }) {

//     let usera = global.DATABASE._data.users[m.sender]
// if (!usera.registered) throw `Anda belum terdaftar, Silahkan ketik ${usedPrefix}daftar nama#username_ig`
//   m.reply(`
// Umur: *${usera.age}thn*
// Umur: *${usera.name}thn*
// Umur: *${usera.ig}thn*
// Umur: *${limit}thn*

// `.trim())
// }
// handler.help = ['daftar', 'reg', 'register'].map(v => v + ' _nama_._umur_')
// handler.tags = ['exp']
// handler.register = true

// handler.command = /^(dd|aaaa?)$/i

// module.exports = handler

